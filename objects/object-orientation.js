// General ideias:

// Procedural Programming: 
//  - Before: non-structured code (go-to);
//  - After: structured code;
// Example: C language.

process(value1, value2, value3);

// Object-Oriented Programming:
// Example: Java, JavaScript language.
object = { // Object has values (attributes) and functions (behaviors).
    value1,
    value2,
    value3,
    process() {
        //...
    }
}

object.process() // Focal point became the object